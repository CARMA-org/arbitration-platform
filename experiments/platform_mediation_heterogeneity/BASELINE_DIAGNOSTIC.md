# Baseline diagnostic: the canonical mixed-bundle evaluation

Generated by `diagnostic_baseline.py` from the canonical raw records at `experiments/platform_mediation/results/raw` plus deterministic workload regeneration. All numbers below are computed, not transcribed. This is a read-only diagnostic; it changes no canonical file.

**Regeneration fidelity.** 400 of 400 canonical `(cell, seed)` scenarios were regenerated with a `scenario_hash` and `workload_hash` matching the raw records exactly, so the regenerated task queues, demands, bounds, and declarations are the same objects the Java harness executed.

Fixed design: 4 resources, 6 agents, 8 tasks/agent (48 tasks per run), two contention levels, 100 seeds/cell, 7 policies.

## 1. Archetype coverage by cell

Count of agents (out of 600 per cell = 6 agents x 100 seeds) whose 8-task queue contains 2, 3, or all 4 archetypes, from the equal-policy records. Regeneration and the raw `archetype` field agree: yes.

| cell | >=2 archetypes | >=3 archetypes | all 4 archetypes (regen = raw) |
|---|---|---|---|
| homogeneous__high | 600 | 576 | 336 = 336 |
| homogeneous__moderate | 600 | 588 | 396 = 396 |
| mixed_bundle__high | 600 | 584 | 377 = 377 |
| mixed_bundle__moderate | 600 | 580 | 356 = 356 |

Full-archetype coverage is *not* itself a measure of cross-agent asymmetry: in a homogeneous cell every agent can contain all four archetypes while every agent in a run is identical. Coverage counts the breadth of a single queue, not the differences between agents.

## 2. Task-mixture dissimilarity by cell

Each agent's normalized 4-element task-frequency vector; per scenario the mean pairwise total variation `0.5*L1`; aggregated over the 100 seeds of each cell.

| cell | mean | median | min | max | p95 |
|---|---|---|---|---|---|
| homogeneous__high | 0.0000 | 0.0000 | 0.0000 | 0.0000 | 0.0000 |
| homogeneous__moderate | 0.0000 | 0.0000 | 0.0000 | 0.0000 | 0.0000 |
| mixed_bundle__high | 0.3368 | 0.3333 | 0.1667 | 0.5000 | 0.4421 |
| mixed_bundle__moderate | 0.3485 | 0.3500 | 0.2167 | 0.5750 | 0.4504 |

## 3. Resource-demand dissimilarity by cell

Each agent's mandatory resource-demand vector normalized to sum to one; per scenario the mean pairwise total variation; aggregated over seeds.

| cell | mean | median | min | max | p95 |
|---|---|---|---|---|---|
| homogeneous__high | 0.0000 | 0.0000 | 0.0000 | 0.0000 | 0.0000 |
| homogeneous__moderate | 0.0000 | 0.0000 | 0.0000 | 0.0000 | 0.0000 |
| mixed_bundle__high | 0.0355 | 0.0345 | 0.0131 | 0.0654 | 0.0564 |
| mixed_bundle__moderate | 0.0376 | 0.0362 | 0.0150 | 0.0830 | 0.0629 |

Across the two mixed cells combined, the mean pairwise resource-demand total variation is **0.0365** (task-mixture combined mean **0.3427**). Resource-demand dissimilarity is roughly 9.4x smaller than task-mixture dissimilarity: agents differ substantially in *which tasks* they run but much less in *aggregate resource shape*.

## 4. Installed-allocation distance from equal quotas

For each cell, seed, agent, and policy, `rel_l1 = sum_r |a_policy - a_equal| / max(sum_r a_equal, 1)` against the paired equal allocation. Means across the two mixed cells:

| policy | mean rel L1 | median | p95 |
|---|---|---|---|
| drf | 11.49% | 9.65% | 27.69% |
| decomposed_cobb_douglas | 4.24% | 3.53% | 9.38% |
| joint_linear | 83.62% | 87.74% | 121.00% |
| joint_cobb_douglas | 4.25% | 3.53% | 9.38% |
| joint_ces | 7.77% | 6.61% | 17.05% |
| joint_leontief | 6.96% | 5.00% | 19.63% |

The three well-specified nonlinear declarations and the decomposed comparator move the installed allocation only single-digit percent away from equal quotas; DRF moves it about ten-plus percent; joint_linear (the misspecification stress test) moves it very far, because linear utility treats the resources as substitutes.

## 5. Effect sizes in task units

Every completion difference is reported both as a completion fraction and as `completion_diff x 48` completed tasks per run. Pooled across the two mixed cells:

| comparison | completion diff | tasks per run |
|---|---|---|
| joint_cobb_douglas_minus_drf | 0.007083 | 0.340 |
| joint_ces_minus_drf | 0.008750 | 0.420 |
| joint_leontief_minus_drf | 0.005000 | 0.240 |
| joint_linear_minus_equal | -0.539375 | -25.890 |
| joint_linear_minus_drf | -0.560521 | -26.905 |

The nonlinear advantages over DRF are on the order of a fraction of one task per 48-task run. These are not large gains.

## 6. Joint-linear floor diagnostics by cell

| cell | frac zero-completion | frac any used resource at floor | frac all used above floor | mean completion (any at floor) | mean completion (all above) | alloc dist from equal | capacity util |
|---|---|---|---|---|---|---|---|
| homogeneous__high | 0.000 | 0.000 | 1.000 | n/a | 0.517 | 0.97% | 0.918 |
| homogeneous__moderate | 0.000 | 0.000 | 1.000 | n/a | 0.733 | 0.64% | 0.942 |
| mixed_bundle__high | 0.768 | 0.965 | 0.035 | 0.038 | 0.298 | 92.96% | 0.058 |
| mixed_bundle__moderate | 0.465 | 0.850 | 0.150 | 0.065 | 0.326 | 74.27% | 0.095 |

Under the unit floor, joint_linear concentrates each agent's allocation onto a single resource and pins the agent's other used resources at their one-unit floor, so bundle-structured tasks that need several resources cannot complete.

## 7. Exploratory association (not causal)

Quartile splits of per-seed policy completion differences against realized resource-demand and task-mixture dissimilarity, by contention and pooled, are in `results/baseline_diagnostic.json` under `quartile_association`. They are descriptive only: quartiles are formed after seeing the data and no threshold is inferred.

## Why the present design sits close to equal quotas

The following are properties of the current experimental design (verified in code and in the numbers above), not implementation defects:

- Eight i.i.d. draws from four archetypes usually give each agent a broad mixture (section 1: most agents contain all four archetypes).
- Three of the four archetypes have similar normalized mandatory resource profiles. Pairwise total variation of the archetype resource profiles:

| archetype pair | resource-profile TV |
|---|---|
| code_review vs doc_processing | 0.0417 |
| research vs doc_processing | 0.0909 |
| research vs code_review | 0.0909 |
| research vs monitoring | 0.2334 |
| doc_processing vs monitoring | 0.3205 |
| code_review vs monitoring | 0.3205 |

- Mean pairwise resource-demand distance (section 3) is much smaller than the task-frequency distance (section 2).
- Every resource capacity is scaled from aggregate mandatory demand by the same contention ratio (`capacity_r = round(total_demand_r / ratio)`), so relative scarcity is uniform across resources.
- Every nonlinear declaration aggregates the agent's complete task queue into one normalized resource vector (`declaration_source = exact_pending_queue`).
- Operator priorities are equal, removing another source of asymmetry.
- Upper bounds and declarations are derived from the exact execution queue.

Together these make an allocation close to equal quotas unsurprising, and they motivate the pilot's manipulation of cross-agent workload concentration.
